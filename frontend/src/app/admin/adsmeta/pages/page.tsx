'use client';
export const dynamic = 'force-dynamic';

import AdsPagesList from '@/components/admin/AdsPagesList';

// Fanpage Meta đã kết nối + quyền (tasks).
export default function AdsPagesPage() {
  return <AdsPagesList />;
}
